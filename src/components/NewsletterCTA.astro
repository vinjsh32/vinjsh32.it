---
interface Props {
  title?: string;
  description?: string;
  buttonText?: string;
  darkBg?: boolean;
}

const { 
  title = "Scatti & Spunti", 
  description = "Iscriviti alla newsletter mensile per ricevere anteprime esclusive, preset gratuiti, backstage e sconti su stampe.", 
  buttonText = "Iscriviti Ora",
  darkBg = false
} = Astro.props;
---

<section class={`py-16 ${darkBg ? 'bg-dark-800 text-white' : 'bg-gray-100 dark:bg-dark-700 text-dark-800 dark:text-white'}`}>
  <div class="container mx-auto px-4 md:px-6">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-3xl font-display font-bold mb-4">{title}</h2>
      <p class="text-lg mb-8 text-gray-700 dark:text-gray-300">{description}</p>
      
      <form class="flex flex-col sm:flex-row gap-3 max-w-xl mx-auto">
        <input 
          type="email" 
          placeholder="La tua email" 
          class="flex-grow px-4 py-3 rounded-md border border-gray-300 dark:border-dark-500 focus:ring-2 focus:ring-primary-600 focus:border-transparent bg-white dark:bg-dark-800"
          required
        />
        <button 
          type="submit" 
          class="px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white font-medium rounded-md transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2"
        >
          {buttonText}
        </button>
      </form>
      
      <p class="text-xs mt-4 text-gray-500 dark:text-gray-400">
        Niente spam, solo ispirazione e valore per la tua fotografia. Puoi cancellarti in qualsiasi momento.
      </p>
    </div>
  </div>
</section>